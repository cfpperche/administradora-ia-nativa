import type { NextConfig } from "next";

const config: NextConfig = {
  // Prototype defaults; per stack-defaults.md, Turbopack is the default bundler in Next.js 16.
  reactStrictMode: true,
};

export default config;
